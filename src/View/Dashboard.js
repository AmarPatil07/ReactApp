function Dashbaord() {
    
    return (
        <div>
            <h1>Dashbaord</h1>
            <p>
            In this example we have 3 “pages” handled by the router: a home page, an about page, and a users page. As you click around on the different s, the router renders the matching 
            </p>
        </div>
    )
}

export default Dashbaord
